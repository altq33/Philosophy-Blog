import React, { ChangeEvent, FormEvent, useContext, useState } from "react";
import auth from "./auth.module.scss";
import { Context } from "../../main";
export const Authorization: React.FC = () => {
  return <div>Я блять авторизация</div>;
};
